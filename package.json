{
  "name": "zenfit",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/backend",
      "apps/mobile",
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**",
      "**/expo",
      "**/expo/**",
      "**/expo-router",
      "**/expo-router/**",
      "**/expo-linking",
      "**/expo-linking/**",
      "**/expo-constants",
      "**/expo-constants/**",
      "**/expo-modules-core",
      "**/expo-modules-core/**",
      "**/expo-splash-screen",
      "**/expo-splash-screen/**",
      "**/nativewind",
      "**/nativewind/**",
      "**/@react-native-community/**",
      "**/@react-navigation/**"
    ]
  },
  "scripts": {
    "mobile": "yarn workspace @zenfit/mobile start",
    "backend": "yarn workspace @zenfit/backend dev",
    "build:backend": "yarn workspace @zenfit/backend build",
    "db:migrate": "yarn workspace @zenfit/backend db:migrate",
    "db:generate": "yarn workspace @zenfit/backend db:generate",
    "db:studio": "yarn workspace @zenfit/backend db:studio",
    "typecheck": "yarn workspaces run typecheck",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "packageManager": "yarn@1.22.22"
}
